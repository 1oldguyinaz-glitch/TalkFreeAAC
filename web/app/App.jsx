import React,{useState}from"react";import ChildAAC from"../child/ChildAAC.jsx";import ParentMenu from"../parent/ParentMenu.jsx";import{loadProfile,saveProfile,resetProfile}from"../shared/profile.js";import{speak}from"../shared/speech.js";import{currentPhrase}from"../../engine/prediction/predictionEngine.js";import{completeSentence}from"../../engine/sentences/sentenceEngine.js";import{addFriction}from"../../engine/friction/frictionLog.js";export default function App(){const[profile,setProfileState]=useState(loadProfile());const[screen,setScreen]=useState("child");function setProfile(p){setProfileState(saveProfile(p));}function tapWord(word){const before=currentPhrase(profile.sentence);const nextSentence=[...profile.sentence,word];const wordCounts={...profile.wordCounts,[word]:(profile.wordCounts[word]||0)+1};const phraseCounts={...profile.phraseCounts,[before]:(profile.phraseCounts[before]||0)+1};const tapHistory=[...profile.tapHistory,{word,phrase:before,time:new Date().toISOString()}].slice(-2000);setProfile({...profile,sentence:nextSentence,wordCounts,phraseCounts,tapHistory,justCompletedSentence:false});speak(word);}function speakSentence(){const phrase=currentPhrase(profile.sentence);if(!phrase)return;speak(phrase);setProfile(completeSentence(profile));}function changeContext(ctx){setProfile({...profile,activeContext:ctx,sentence:[],justCompletedSentence:false});}return <div className="app">{screen==="child"?<ChildAAC profile={profile} setProfile={setProfile} onTap={tapWord} onSpeak={speakSentence} onComplete={speakSentence} onBack={()=>setProfile({...profile,sentence:profile.sentence.slice(0,-1)})} onClear={()=>setProfile({...profile,sentence:[]})} onContext={changeContext} onFriction={issue=>setProfile(addFriction(profile,issue))} onParent={()=>setScreen("parent")}/>:<ParentMenu profile={profile} setProfile={setProfile} onBack={()=>setScreen("child")}/>}</div>}