import React from"react";import{buildInsightSummary}from"../../engine/insights/insightEngine.js";export default function ParentMenu({profile,setProfile,onBack}){const s=buildInsightSummary(profile);const u=profile.userProfile||{};return <div><div className="top"><div><div className="brand">🔒 Parent Menu</div><div>Profile • Display Settings • Insights • Team Workspace</div></div><button className="lock" onClick={onBack}>AAC</button></div><div className="panel"><h2>User Profile + Emergency</h2><input className="search" placeholder="Preferred name" value={u.name||""} onChange={e=>setProfile({...profile,userProfile:{...u,name:e.target.value}})}/><textarea className="search" placeholder="Emergency description" value={u.emergencyDescription||""} onChange={e=>setProfile({...profile,userProfile:{...u,emergencyDescription:e.target.value}})}/><input className="search" placeholder="Allergies" value={u.allergies||""} onChange={e=>setProfile({...profile,userProfile:{...u,allergies:e.target.value}})}/><input className="search" placeholder="Medical notes" value={u.medicalNotes||""} onChange={e=>setProfile({...profile,userProfile:{...u,medicalNotes:e.target.value}})}/></div><div className="panel"><h2>Display Settings</h2><p>TalkFree Pop color language locked: emotions red, food orange, learning yellow, places green, people blue, questions purple, actions lime, core navy.</p></div><div className="panel"><h2>Insights</h2><div className="metrics"><div className="metric"><strong>Taps</strong><span>{s.totalTaps}</span></div><div className="metric"><strong>Sentences</strong><span>{s.completedSentences}</span></div><div className="metric"><strong>Friction</strong><span>{s.frictionCount}</span></div><div className="metric"><strong>Words</strong><span>{s.topWords.length}</span></div></div></div><div className="panel"><h2>Team Workspace</h2><p>Opt-in only. Teachers/therapists can add notes later. AAC communication data remains local.</p></div></div>}