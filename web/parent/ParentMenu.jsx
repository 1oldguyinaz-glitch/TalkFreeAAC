import React from"react";import{buildInsightSummary}from"../../engine/insights/insightEngine.js";export default function ParentMenu({profile,setProfile,onBack}){const s=buildInsightSummary(profile);return <div><div className="top"><div><div className="brand">🔒 Parent Menu</div><div>Profile • Display Settings • Insights</div></div><button className="lock" onClick={onBack}>AAC</button></div><div className="panel"><h2>User Profile</h2><input className="search" placeholder="Preferred name" value={profile.userProfile?.name||""} onChange={e=>setProfile({...profile,userProfile:{...profile.userProfile,name:e.target.value}})}/><textarea className="search" placeholder="Emergency description" value={profile.userProfile?.emergencyDescription||""} onChange={e=>setProfile({...profile,userProfile:{...profile.userProfile,emergencyDescription:e.target.value}})}/></div><div className="panel"><h2>Display Settings</h2><p>TalkFree color language locked. Future controls: contrast, colorblind mode, button size, text size.</p></div><div className="panel"><h2>Insights</h2><div className="metrics"><div className="metric"><strong>Taps</strong><span>{s.totalTaps}</span></div><div className="metric"><strong>Sentences</strong><span>{s.completedSentences}</span></div><div className="metric"><strong>Friction</strong><span>{s.frictionCount}</span></div><div className="metric"><strong>Words</strong><span>{s.topWords.length}</span></div></div></div></div>}