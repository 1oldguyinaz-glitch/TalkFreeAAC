import React,{useMemo,useState}from"react";import{getPredictions,currentPhrase}from"../../engine/prediction/predictionEngine.js";import{KNOWLEDGE_OBJECTS,TYPE_STYLES,GENOME}from"../../engine/genome/genome.js";import{searchObjects}from"../../engine/search/searchEngine.js";export default function ChildAAC({profile,setProfile,onTap,onSpeak,onComplete,onBack,onClear,onContext,onFriction,onParent}){const[query,setQuery]=useState("");const section=profile.activeContext;const predictions=useMemo(()=>section==="Search"&&query?searchObjects(query).map(o=>o.name):getPredictions(profile),[profile,query,section]);const phrase=currentPhrase(profile.sentence||[]);const rail=GENOME.child_visible_sections;return <div className="shell"><div className="rail">{rail.map(r=><button className={section===r?"active":""} key={r} onClick={()=>{setQuery("");onContext(r)}}>{r}</button>)}</div><main><div className="top"><div><div className="brand">TalkFreeAAC</div><div>Free communication • local-first</div></div><button className="lock" onClick={onParent}>🔒</button></div>{profile.lastCompletedSentence?<div className="last">Last said: {profile.lastCompletedSentence}</div>:null}<div className="sentence">{phrase||"\u00A0"}</div><div className="controls"><button className="control" onClick={onSpeak}>🔊 Speak</button><button className="control" onClick={onComplete}>✅ Done</button><button className="control" onClick={onBack}>⬅️ Back</button><button className="control" onClick={onClear}>❌ Clear</button><button className="control" onClick={()=>onFriction("Could not find word")}>⚠️ Hard</button></div>{section==="Search"?<input className="search" placeholder="Search words..." value={query} onChange={e=>setQuery(e.target.value)}/>:null}<div className="board-title">{profile.justCompletedSentence?"New thought":section}</div><div className="grid">{predictions.map(word=>{const obj=KNOWLEDGE_OBJECTS[word];const style=TYPE_STYLES[obj?.grammar_type]||TYPE_STYLES.object;return <button key={word} className="word" style={{background:style.color}} onClick={()=>onTap(word)}><div className="icon">{obj?.icon||"🔤"}</div><div className="label">{word}</div></button>})}</div></main></div>}